import React from 'react';
import { Typography, Box } from '@mui/material';

function FlowMode() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Flow Mode
      </Typography>
      <Typography variant="body1">
        Distraction-free focus mode. Coming soon in E3-S1.
      </Typography>
    </Box>
  );
}

export default FlowMode;
